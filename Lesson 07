// Traversing the DOM pt 1

const bookList = document.querySelector('#book-list');
console.log('the parent node is', bookList.parentNode);
console.log('the parent element is', bookList.parentElement.parentElement); //do the same thing as parent node, 
//then find parent element of first parent element

console.log(bookList.children) //get element children, and NOT nodes
